
export enum UserType {
    ADMIN = 'admin',
    MEMBER = 'member',
    OWNER = 'owner',
}

export enum InvitationStatus {
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    EXPIRED = 'expired',
}